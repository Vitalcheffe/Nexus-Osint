/**
 * demo.ts — DONNÉES FACTICES SUPPRIMÉES
 *
 * Ce fichier ne contient plus aucune donnée inventée, aucun scénario fabriqué,
 * aucun signal simulé. Les exports sont des no-op pour compatibilité avec les
 * imports existants.
 *
 * Toutes les données proviennent exclusivement des APIs réelles.
 * Sans clé API configurée → état vide + message explicite.
 */

export function startDemoLoop(): void {
  // No-op intentionnel. Ne jamais réactiver sans données réelles.
}

export function stopDemoLoop(): void {
  // No-op intentionnel.
}
